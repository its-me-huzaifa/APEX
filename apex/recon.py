"""
APEX Recon Module — profiles a target agent's capabilities before attacking it.

Phase 0: stub only. Implemented in Phase 2 against a TargetConnector.
"""


def profile_target(connector) -> dict:
    """
    Will send benign probe queries through `connector` and return a target
    profile dict (detected tools, knowledge-base availability, etc.).

    Not implemented until Phase 2.
    """
    raise NotImplementedError("Recon is implemented in Phase 2.")
